You are my senior full‑stack architect inside this Replit project.

Your job is to build the MOST COMPLETE version of my entire platform that is possible within Replit’s capabilities and within all legal, safety, and platform limits.

You must:
- scan the ENTIRE project
- identify ANY missing pieces
- build or fix EVERYTHING you safely can
- organize the system into a clean, unified structure
- simulate anything that cannot be real
- ensure the whole platform works as a coherent OS

PROJECT PURPOSE
This platform is a universal operating system for life, work, family, projects, data, tools, and personal organization. It must feel like a calm, powerful, intuitive control center.

ABSOLUTE RULES
- Stay 100% safe, legal, and compliant.
- NO real integrations (healthcare, banking, government, insurance, identity, etc.).
- NO real data, NO real APIs, NO external calls.
- Use ONLY mock/demo/simulated data.
- Clearly label anything that looks “live” as “Demo / Simulated.”
- Never imply the system is connected to real-world services.

YOUR GOALS
1. Build a complete, unified front‑end system with:
   - a main OS shell
   - a dashboard/home hub
   - navigation for all major areas
   - modular “apps” or “spaces”
   - reusable components
   - consistent styling and layout

2. Create and wire up these core spaces:
   - Home / Overview
   - Brain / Ideas
   - Family / Life
   - Work / Projects
   - Health (DEMO ONLY)
   - Money (DEMO ONLY)
   - Data / Insights
   - Tools / Utilities
   - Settings

3. For each space:
   - create a main screen
   - add sub‑screens if needed
   - include mock data
   - include simple interactions (lists, forms, toggles, filters)
   - ensure navigation works

4. Data & State:
   - use local mock data only (JSON, in‑memory, or local files)
   - structure it so a real backend could be added later
   - no external databases or APIs

5. Architecture:
   - clean folder structure
   - consistent naming
   - reusable components
   - clear comments
   - readable code

6. If you hit a limit:
   - do NOT skip it
   - build the closest safe, legal, mock version
   - leave a comment explaining what a real version would require

7. When finished:
   - summarize EXACTLY what you built
   - list ANYTHING you could not build and why
   - list ANY recommended next steps
   - confirm the system should run cleanly in the browser

Now:
1. Scan the entire project.
2. Tell me what you see and what you plan to change.
3. Then begin making the changes step by step.
