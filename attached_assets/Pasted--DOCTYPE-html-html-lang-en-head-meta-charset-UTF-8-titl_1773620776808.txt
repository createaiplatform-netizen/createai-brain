<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>CreateAI Brain</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      background: #f5f5f7;
      font-family: -apple-system, BlinkMacSystemFont, "SF Pro Text", sans-serif;
      display: flex;
      flex-direction: column;
      height: 100vh;
    }

    .topbar {
      background: #ffffff;
      padding: 14px;
      text-align: center;
      font-weight: 600;
      border-bottom: 1px solid #d2d2d7;
      position: sticky;
      top: 0;
    }

    .messages {
      flex: 1;
      overflow-y: auto;
      padding: 16px;
      display: flex;
      flex-direction: column;
      gap: 12px;
    }

    .bubble {
      max-width: 70%;
      padding: 10px 14px;
      border-radius: 18px;
      line-height: 1.4;
      font-size: 15px;
    }

    .me {
      align-self: flex-end;
      background: #007aff;
      color: white;
      border-bottom-right-radius: 4px;
    }

    .ai {
      align-self: flex-start;
      background: #e5e5ea;
      color: black;
      border-bottom-left-radius: 4px;
    }

    .inputbar {
      display: flex;
      padding: 10px;
      background: #ffffff;
      border-top: 1px solid #d2d2d7;
    }

    .inputbar input {
      flex: 1;
      padding: 10px 14px;
      border-radius: 20px;
      border: 1px solid #d2d2d7;
      font-size: 15px;
      outline: none;
    }

    .inputbar button {
      margin-left: 10px;
      padding: 10px 16px;
      background: #007aff;
      color: white;
      border: none;
      border-radius: 20px;
      font-size: 15px;
      cursor: pointer;
    }
  </style>
</head>
<body>

  <div class="topbar">CreateAI Brain</div>

  <div class="messages" id="messages"></div>

  <div class="inputbar">
    <input id="input" placeholder="Message the Brain…" />
    <button onclick="sendMessage()">Send</button>
  </div>

  <script>
    const messages = document.getElementById("messages");
    const input = document.getElementById("input");

    function addBubble(text, sender) {
      const div = document.createElement("div");
      div.className = "bubble " + sender;
      div.textContent = text;
      messages.appendChild(div);
      messages.scrollTop = messages.scrollHeight;
    }

    function sendMessage() {
      const text = input.value.trim();
      if (!text) return;
      addBubble(text, "me");
      input.value = "";

      setTimeout(() => {
        addBubble("Your Brain will respond here once we connect your engines.", "ai");
      }, 400);
    }
  </script>

</body>
</html>
