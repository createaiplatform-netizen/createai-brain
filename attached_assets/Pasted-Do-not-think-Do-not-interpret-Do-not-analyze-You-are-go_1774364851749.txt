Do not think. Do not interpret. Do not analyze.

You are going to FINISH the full‑potential package for this platform.

You have already implemented or will implement:
- fullPotentialActivation.ts
- /api/system/full-auto-create
- FullAutoCreatePage
- Integration with commandProcessor

Now complete the entire package exactly as described below.

========================================
1) FULL POTENTIAL ACTIVATION MODULE
========================================

File: src/services/fullPotentialActivation.ts (or the closest matching path in this repo)

Responsibilities:
- Accept a natural language request (string + optional context).
- Detect domain(s), intent(s), and scope:
  - Domains: healthcare, construction, energy, government, nonprofit, manufacturing, transportation, family, finance, education, hospitality, insurance, etc.
  - World-building: cosmology, civilization, lore, mythology, tech, timeline, ecology, game world, language, war, visual world, urban world.
  - Meta/intelligence: strategy, planning, insight, brainstorming, orchestration.
- Select relevant engines:
  - Vertical engines (e.g., healthcareEngine, constructionEngine, energyEngine, etc.).
  - World-building engines (e.g., cosmologyForge, civilizationForge, loreForge, etc.).
  - Intelligence engines (brain, intelligenceOracle, insightEngine, aiStrategy, imagination, brainstorm).
  - Platform OS engines (coreOS, orchestrator, maximizer, narratorOS, bundleOS, creationEngines).
- Use universe OS:
  - Assign identities, layers, relationships, activation logic, meaning, narrative, continuity.
  - Use existing universe manifests and layers (no new ones).
- Use existing project-builder / project creation logic (if present) to assemble a project.

Output shape (TypeScript type or equivalent):

type FullAutoProject = {
  id: string;
  title: string;
  domain: string[];
  description: string;
  enginesUsed: string[];
  universe: {
    identities: string[];
    layers: string[];
    relationships: string[];
    activationChain: string[];
    meaningThreads: string[];
    narrativeHooks: string[];
  };
  workflows: {
    name: string;
    steps: string[];
    engines: string[];
  }[];
  dataModel: {
    entities: {
      name: string;
      description: string;
      relatedTables?: string[];
    }[];
  };
  recommendations: {
    nextSteps: string[];
    risks: string[];
    opportunities: string[];
  };
  scoringSnapshot: {
    readiness: number;
    completeness: number;
    stability: number;
    integration: number;
    performance: number;
    security: number;
    scalability: number;
  };
  worldBuilding?: {
    elements: string[];
    timelines?: string[];
    locations?: string[];
    characters?: string[];
  };
  createdAt: string;
};

The module must export a function like:

async function runFullAutoCreate(input: {
  prompt: string;
  userId?: string;
}): Promise<FullAutoProject>

Implementation rules:
- Use existing engines and services; do not invent new subsystems.
- Use getPlatformScores() or equivalent to populate scoringSnapshot.
- Use universe OS helpers (continuum, manifests, layers) to populate universe.* fields.
- Use existing domain/engine registries where possible.
- Do not fabricate capabilities that do not exist in the codebase.

========================================
2) COMMAND PROCESSOR INTEGRATION
========================================

File: src/services/commandProcessor.ts (or equivalent)

Add a new command handler:

- Name: "FULL_AUTO_CREATE"
- Trigger patterns (examples):
  - "create a system for"
  - "build a project for"
  - "generate a platform for"
  - "set up a solution for"
  - "make a full system for"
  - "design a universe for"
- Behavior:
  - When a user input matches these patterns, call runFullAutoCreate({ prompt: originalInput, userId }).
  - Log the action in existing audit / command logs.
  - Return the FullAutoProject object as the primary payload.

Ensure:
- FULL_AUTO_CREATE is added to COMMAND_HANDLERS[] or equivalent registry.
- The handler is available to authenticated users (respect existing auth/scope model).

========================================
3) API ENDPOINT
========================================

File: src/routes/system/fullAutoCreate.ts (or closest match)

Create a new authenticated route:

- Method: POST
- Path: /api/system/full-auto-create
- Auth: same as other /api/* authenticated routes (authMiddleware + scopeMiddleware).
- Request body:
  {
    "prompt": string
  }
- Response body:
  {
    "project": FullAutoProject
  }

Behavior:
- Validate that "prompt" is a non-empty string.
- Call runFullAutoCreate({ prompt, userId: req.user.id }).
- Return the result as { project }.
- Handle errors gracefully with existing error handling patterns.

========================================
4) FRONTEND PAGE: FullAutoCreatePage
========================================

File: src/pages/FullAutoCreatePage.tsx (or equivalent route file)

Route:
- Path: /full-auto-create (or /projects/auto-create if that fits better with existing routing)
- Gated: authenticated (use existing OSLayout / AppShell)

Layout (Cosmic OS Minimalism, consistent with existing design):

Sections:

1) Header
- Title: "Full Auto Create"
- Subtitle: "Describe anything. The platform assembles a complete system."
- Show a small badge: "Powered by all engines + Universe OS"

2) Input Panel
- Large text area for the user to describe:
  - an idea
  - a business
  - a workflow
  - a world
  - a problem
  - a domain
- Button: "Generate System"
- Optional: small helper text with examples:
  - "Create a healthcare intake and follow-up system for a small clinic."
  - "Build a construction project tracking system for residential builds."
  - "Design a world and economy for a cooperative sci-fi game."
  - "Generate a nonprofit donor and grant management system."

3) Result Panel
Once the API returns a FullAutoProject:

- Show:
  - Project title
  - Domains detected
  - Short description
  - Engines used (chips/badges)
- Tabs or sections:
  - "Workflows"
    - List each workflow with steps.
  - "Universe"
    - Show identities, layers, relationships, activation chain, meaning threads, narrative hooks.
  - "Data Model"
    - List entities and related tables.
  - "Recommendations"
    - Next steps, risks, opportunities.
  - "World"
    - If worldBuilding is present, show elements, timelines, locations, characters.
  - "Scores"
    - Readiness, completeness, stability, integration, performance, security, scalability (use existing score visual components if available).

4) System Status Sidebar (optional but preferred)
- Reuse components from PlatformScorePage / PlatformStatusPage if possible.
- Show current platform scores and a small "System Ready" indicator.

Behavior:
- On submit:
  - Call POST /api/system/full-auto-create with { prompt }.
  - Show loading state.
  - Render the returned project.
- Handle errors with a clear message and retry option.

========================================
5) NATURAL LANGUAGE TRIGGERS (FRONTEND)
========================================

If there is a GlobalCommandPalette or CommandCenter:

- Add a new action:
  - Name: "Full Auto Create"
  - Description: "Generate a complete system from any idea."
  - When selected:
    - Open FullAutoCreatePage
    - Optionally pre-fill the prompt if text was selected.

========================================
6) SAFETY AND LEGAL CONSTRAINTS
========================================

In fullPotentialActivation.ts and related flows:

- Do NOT:
  - Generate real financial transactions.
  - Generate real medical advice or treatment plans.
  - Bypass existing safetyGuard.ts or equivalent.
- DO:
  - Use existing safety layers.
  - Keep outputs at the level of workflows, structures, and systems, not prescriptions or diagnoses.
  - Respect the virtual-only nature of Family Bank and any similar constructs.

========================================
7) FINAL INTEGRATION
========================================

- Ensure:
  - The new route is registered in the API router.
  - The new page is registered in the frontend router.
  - The commandProcessor handler is wired and reachable.
  - Types are consistent with existing patterns.
  - No existing behavior is broken.

No summaries. No commentary. No extra text. Only implement exactly what is described above.
