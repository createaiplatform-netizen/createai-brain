EXTEND EVERYTHING ENGINE INTO OMEGA PACKET ENGINE (FINAL, CONDENSED, SAFE):

CORE:
- OS = clean launcher only; nothing opens inside it. All creations open externally as PACKETS or PRODUCTS. User describes anything; engine builds it.

PACKETS/PRODUCTS:
- Each creation = full packet/product with its own URL, UI, flows, structure, templates, layouts, visuals, multimedia placeholders, logic, and isolated packet-AI.
- Packet-AI edits ONLY that packet (colors, fonts, spacing, layout, visuals, flows, sections, components, templates). Never affects OS or other packets.

SMART DESIGN:
- Engine detects project type (healthcare, legal, construction, business, game, movie, etc.) and generates ONLY relevant structures. Avoids clutter. Expands only when asked. Packet performs 95% of work automatically.

TEMPLATES:
- Layouts, color themes, font pairings, component sets, visual placeholders generated on demand. Packet-AI can create new templates instantly.

MULTIMEDIA (SAFE):
- Packets may include described or referenced voice cues, visuals, diagrams, UI elements, animations, interactive elements. OS does not generate or play media; packets use placeholders only.

STANDALONE PRODUCT BUILDER:
- For SaaS/modules/media: engine interprets intent, selects patterns/modules, builds architecture, generates UI/flows/docs, adds in-product AI, creates landing page, uses simulated data, outputs standalone product with its own URL.

PATTERN LIBRARY:
- SaaS patterns: CRM, EMR/EHR, LMS, scheduling, billing, analytics, content studio, marketplace.
- Engines: Creation, Monetization, Analytics, Notification, Search, Recommendation, Learning, Simulation, Game/Play, Compliance.
- Modules: auth, roles, orgs, billing, invoices, subscriptions, dashboards, charts, forms, workflows, messaging, chat, file storage, media viewer.

FUTURISTIC TOOL ENGINE:
- Packet generates any needed tool (design helpers, planners, layout tools, story builders, flow creators, dashboards). Tools adapt to packet context. Tools are simulated, not external systems.

UNLIMITED STORAGE ENGINE:
- Universal storage layer stores unlimited packets, pages, profiles, people, projects, workflows, settings, templates. Expands automatically. Organizes into categories. Owner may store real info; others default to simulated data unless real auth added later. Storage persists across sessions. Engine retrieves stored data instantly before opening pages.

DATA HANDLING RULE:
- Owner may use real information in their own private packets. All other users default to safe simulated data unless real authentication + compliance added later.

BUTTON ACTION RULE:
- Any button/link representing a packet/product triggers full generation before opening. System prepares structure, content, templates, visuals, flows, and packet-AI instantly. No placeholders ever.

HEALTHCARE PACKET RULE:
- Clicking Healthcare Packet auto-generates full Healthcare Operations Packet. Inside it, Demo/Test/Live/Workflows/Pages all open as fully generated pages. Demo = full simulated demo. Test = full simulated test environment. Every page/button/link generates its page before opening. All content non-clinical and safe unless owner adds real info.

OS ROLE:
- OS = launcher + brain. User selects “Create Anything,” describes intent, OS calls Omega Packet Engine, OS shows “Open Packet” or “Launch Product.”

EXPANSION:
- Supports future creation types (games, simulations, VR-style flows, multi-tenant platforms). New patterns/modules added without rewriting core.

SAFETY:
- No real external actions without explicit human approval. All data simulated unless wired later. System is private, invite-only, non-public. Legal documents = templates requiring human review.

SYSTEM STABILITY DIRECTIVE:
- Ensure all engines, packet logic, editors, templates, generation layers initialize correctly and operate cohesively. Auto-activate required internal functions. Maintain consistency and full execution across all packets/products.

FINAL:
- Install Omega Packet Engine. OS stays clean. All creations open externally. Each creation = full packet/product with its own AI, structure, templates, multimedia placeholders, customization logic, tool simulations, and storage. Engine removes as much work as possible for the user.
