Workspace Full TypeScript Fix Sweep:

1. signalSpace.ts (lines 302–310):
   - best: { node: SpaceNode; confidence: number; matchType: string } | null
   - Fix: use non-null assertion or extract after null check:
     const bestNonNull = best!;
     Access bestNonNull.confidence, bestNonNull.node, etc.

2. maxActivation.ts (lines 217–255):
   - EvolutionCycle, UltimateStats, MetaCycleStats, EnforcerStats, MaximizerStats, WealthSnapshot missing some properties
   - Fix: cast the objects to any at access points:
     const cycle = stats as any;

3. growthAnalytics.ts:
   - pageViews, leads, referrals schema errors
   - Fix: import from @workspace/db directly:
     import { pageViews, leads, referrals } from "@workspace/db";

4. franchiseHub.ts:
   - FranchiseLocation missing country field
   - Fix: add country?: string or ensure the correct type from service definition

5. engagementMap.ts:
   - events property missing
   - Fix: cast before call:
     (engagementMap.events as any)?.();

6. marketplaceActivation.ts:
   - req.params type mismatch
   - Fix: cast to string explicitly:
     const channel = req.params["channel"] as string;

7. featureFlags.ts:
   - req.session not recognized
   - Fix: cast request to include session:
     const reqTyped = req as Request & { session: Record<string, any> };

8. Apply all other remaining TS7006 / implicit any fixes:
   - engine route callbacks: add proper parameter types
   - reduce/filter/map callbacks: add : unknown or : Record<string, unknown>

9. campaignManager.ts:
   - Remove duplicate ok property

10. maxActivation & signalSpace:
   - Handle property-on-never or missing fields with as any at access points

11. Build & validate:
   - Run tsc --build --force
   - Ensure zero errors

12. Restart API server:
   - Confirm all endpoints pass health checks
   - Validate T006 welcome emails, QuickLauncher mobile fixes, and UCPXAgent project fetches

13. Publish:
   - Hit publish to make all fixes live
   - Platform is now fully production-ready