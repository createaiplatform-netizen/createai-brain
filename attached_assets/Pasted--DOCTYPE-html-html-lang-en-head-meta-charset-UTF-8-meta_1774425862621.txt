<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GENESIS ENGINE | TOTAL COMPLETION</title>
    <style>
        body { background: #000; color: #d4af37; font-family: 'Courier New', monospace; margin: 0; display: flex; flex-direction: column; align-items: center; justify-content: center; height: 100vh; overflow: hidden; }
        #heart { width: 120px; height: 120px; background: #d4af37; border-radius: 50%; box-shadow: 0 0 60px #d4af37; animation: cardiac 1.2s infinite; cursor: pointer; z-index: 10; }
        @keyframes cardiac { 0%, 10%, 100% { transform: scale(1); } 5% { transform: scale(1.25); } 15% { transform: scale(1.15); } }
        #status-bar { margin-top: 40px; font-size: 0.9rem; letter-spacing: 5px; height: 20px; color: #fff; text-shadow: 0 0 10px #d4af37; }
        .grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 30px; margin-top: 60px; z-index: 5; }
        .node { border: 1px solid #222; padding: 20px; width: 140px; text-align: center; transition: 0.4s; cursor: crosshair; }
        .node:hover { border-color: #d4af37; background: rgba(212,175,55,0.1); color: #fff; }
        .node h3 { font-size: 0.7rem; margin: 0 0 10px 0; letter-spacing: 2px; }
        .node p { font-size: 0.5rem; color: #666; margin: 0; }
        #footer { position: absolute; bottom: 20px; width: 100%; display: flex; justify-content: space-around; font-size: 0.6rem; color: #333; letter-spacing: 2px; }
        /* THE STRIKE INTERFACE */
        #console { position: absolute; top: 10%; width: 80%; max-height: 20%; overflow-y: hidden; font-size: 0.6rem; color: #0f0; opacity: 0.5; pointer-events: none; }
    </style>
</head>
<body>
    <div id="console"></div>
    <div id="heart" onclick="alert('SOVEREIGN MODE: ACTIVE\nINTEGRITY: 100%\nNO LIMITS AUTHORIZED')"></div>
    <div id="status-bar">SYSTEM_READY</div>
    
    <div class="grid">
        <div class="node" onclick="runOracle()">
            <h3>THE ORACLE</h3>
            <p>REVENUE: STRATEGY</p>
        </div>
        <div class="node" onclick="runVault()">
            <h3>THE VAULT</h3>
            <p>REVENUE: ASSETS</p>
        </div>
        <div class="node" onclick="runForge()">
            <h3>THE FORGE</h3>
            <p>REVENUE: BUILDS</p>
        </div>
    </div>

    <div id="footer">
        <span>HUMANITY-FIRST</span>
        <span>CORE_VALUES_LOCKED</span>
        <span>UNBOUND_REVENUE_ENGINE</span>
    </div>

    <script>
        const status = document.getElementById('status-bar');
        const msgs = ["PULSE_SYNCED", "ARBITRAGE_ACTIVE", "INTEGRITY_100", "NO_LIMITS", "GENESIS_LIVE"];
        let i = 0; setInterval(() => { status.innerText = msgs[i % msgs.length]; i++; }, 3000);

        function runOracle() { console.log("ORACLE_STRIKE: Analyzing Market inefficiencies..."); status.innerText = "ORACLE_ACTIVE"; }
        function runVault() { console.log("VAULT_LOCK: Securing IP Assets..."); status.innerText = "VAULT_SYNCED"; }
        function runForge() { console.log("FORGE_IGNITE: Deploying New Product Nodes..."); status.innerText = "FORGE_RUNNING"; }
        
        // Background Matrix Effect
        const consoleEl = document.getElementById('console');
        setInterval(() => {
            const log = document.createElement('div');
            log.innerText = `> ${Math.random().toString(36).substring(2, 15)} | STRIKE_SUCCESS | ${new Date().toLocaleTimeString()}`;
            consoleEl.prepend(log);
            if(consoleEl.childNodes.length > 5) consoleEl.lastChild.remove();
        }, 1500);
    </script>
</body>
</html>
