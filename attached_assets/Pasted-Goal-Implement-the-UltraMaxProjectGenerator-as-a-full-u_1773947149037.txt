Goal: Implement the UltraMaxProjectGenerator as a **full, unified project pipeline**.

Instructions:
1. Wire the UltraMaxProjectGenerator blueprint as the **new unified backend entry point**:
   • Replace both `/api/movie/generate` and `/api/render/generate` with a single `/api/generate` route.
   • Ensure the route accepts projectType, projectId, and scaffoldFiles.
   • Stream structured progress events (status, scene_done, partial_manifest, done, error) via SSE to the frontend.

2. Implement all 10 UltraMax steps fully:
   • Validation of generated HTML/JS/game/app code.
   • Partial manifest checkpointing per scene/step.
   • Branching choices generated by GPT per pivotal scene/event.
   • Web Audio arpeggiator integrated per project type and mood.
   • Inline editing for all generated content in frontend players (BookReader, CoursePlayer, PodcastPlayer).
   • ElevenLabs TTS integration for character-specific voices where applicable.
   • Deployable URLs for HTML5 games and app prototypes.
   • Session resume — reopen modal and restore previous session/manifest.
   • DALL-E prompt enhancement with cinematographic metadata.
   • Responsive UI for mobile and desktop for all players and scaffold pickers.

3. Frontend integration:
   • Connect RenderEngineApp and CinematicPlayer to `/api/generate`.
   • Show live progress via progress logs and transport UI.
   • Auto-play, interactive branching, TTS, ambient audio, and inline editing should function fully.
   • Ensure iframe players, video/audio players, and app/game previews are all sandboxed but functional.

4. Scope: **Full implementation of all steps**, no partial work — the goal is a fully playable/watchable/deployable project pipeline for any project type.

5. Typecheck & run frontend:
   • Fix any import/export or port-detection issues.
   • Confirm all TypeScript checks pass before finalizing the build.

Execute all of the above as a single, end-to-end workflow so that **any new project generated through the platform is immediately complete, interactive, and fully functional**, with live progress streaming and session persistence.