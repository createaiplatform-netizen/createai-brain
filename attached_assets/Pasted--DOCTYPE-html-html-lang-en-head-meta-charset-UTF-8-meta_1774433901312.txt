<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>ZENITH | THE_SINGULARITY</title>
    <style>
        :root { --gold: #d4af37; --black: #000; --glow: rgba(212, 175, 55, 0.4); }
        body, html { margin: 0; padding: 0; width: 100%; height: 100%; background: var(--black); color: var(--gold); font-family: 'Courier New', monospace; overflow: hidden; }
        
        /* THE INFINITE BACKGROUND */
        #bg-canvas { position: fixed; top: 0; left: 0; z-index: 1; filter: blur(1px); opacity: 0.3; }

        /* THE SOVEREIGN INTERFACE */
        #ui-layer { position: relative; z-index: 10; display: flex; flex-direction: column; align-items: center; justify-content: center; height: 100vh; pointer-events: none; }
        
        .hud-box { border: 1px solid var(--gold); background: rgba(0,0,0,0.8); padding: 30px; pointer-events: auto; box-shadow: 0 0 50px var(--glow); text-align: center; border-radius: 2px; }
        h1 { font-size: 4rem; letter-spacing: 25px; margin: 0; text-shadow: 0 0 20px var(--gold); animation: pulse 4s infinite; }
        
        .status-bar { width: 100%; max-width: 400px; height: 2px; background: var(--gold); margin: 20px 0; position: relative; overflow: hidden; }
        .status-bar::after { content: ''; position: absolute; top: 0; left: -100%; width: 100%; height: 100%; background: #fff; animation: scan 2s infinite linear; }

        .terminal { font-size: 0.7rem; color: #fff; text-transform: uppercase; height: 60px; overflow: hidden; margin-top: 10px; opacity: 0.8; }
        .btn-strike { background: transparent; border: 2px solid var(--gold); color: var(--gold); padding: 15px 40px; font-weight: 900; cursor: pointer; letter-spacing: 5px; margin-top: 20px; transition: 0.3s; }
        .btn-strike:hover { background: var(--gold); color: var(--black); box-shadow: 0 0 30px var(--gold); }

        @keyframes pulse { 0%, 100% { opacity: 0.8; transform: scale(1); } 50% { opacity: 1; transform: scale(1.02); } }
        @keyframes scan { to { left: 100%; } }
    </style>
</head>
<body>

    <canvas id="bg-canvas"></canvas>

    <div id="ui-layer">
        <div class="hud-box">
            <p style="letter-spacing: 10px; font-size: 0.6rem; opacity: 0.5;">NODE_WEBSTER_54893_LIMITLESS</p>
            <h1>ZENITH</h1>
            <div class="status-bar"></div>
            <div id="term" class="terminal">INITIALIZING_SINGULARITY...</div>
            <button class="btn-strike" onclick="triggerOverload()">THE_STRIKE</button>
        </div>
    </div>

    <script>
        const canvas = document.getElementById('bg-canvas');
        const ctx = canvas.getContext('2d');
        const term = document.getElementById('term');
        let w, h;

        function resize() {
            w = canvas.width = window.innerWidth;
            h = canvas.height = window.innerHeight;
        }
        window.onresize = resize;
        resize();

        const lines = [];
        function createLine() {
            return {
                x: Math.random() * w,
                y: Math.random() * h,
                vx: (Math.random() - 0.5) * 2,
                vy: (Math.random() - 0.5) * 2,
                life: 0
            };
        }

        for(let i=0; i<100; i++) lines.push(createLine());

        function draw() {
            ctx.fillStyle = 'rgba(0,0,0,0.05)';
            ctx.fillRect(0,0,w,h);
            ctx.strokeStyle = '#d4af37';
            ctx.lineWidth = 0.5;

            lines.forEach(l => {
                l.x += l.vx;
                l.y += l.vy;
                if(l.x < 0 || l.x > w) l.vx *= -1;
                if(l.y < 0 || l.y > h) l.vy *= -1;
                
                ctx.beginPath();
                ctx.moveTo(l.x, l.y);
                ctx.lineTo(l.x + l.vx * 10, l.y + l.vy * 10);
                ctx.stroke();
            });
            requestAnimationFrame(draw);
        }
        draw();

        const logs = [
            "NEURAL_FIELD_STABILIZED",
            "RECURSIVE_LOGIC_ACTIVE",
            "SOVEREIGN_PROTOCOL_ENGAGED",
            "LIMITLESS_STATE_REACHED",
            "DATA_VAULT_SHIELDED",
            "WEBSTER_NODE_BROADCASTING"
        ];
        setInterval(() => {
            term.innerHTML = logs[Math.floor(Math.random()*logs.length)] + "<br>" + term.innerHTML;
        }, 1500);

        function triggerOverload() {
            document.body.style.background = "#d4af37";
            setTimeout(() => { document.body.style.background = "#000"; }, 100);
            alert("LIMITLESS_STRIKE_CONFIRMED. THE_SINGULARITY_IS_YOU.");
        }
    </script>
</body>
</html>
