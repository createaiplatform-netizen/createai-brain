<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SOVEREIGN GENESIS | CORE 1.0</title>
    <style>
        body { background: #000; color: #d4af37; font-family: 'Courier New', monospace; display: flex; flex-direction: column; align-items: center; justify-content: center; height: 100vh; margin: 0; overflow: hidden; }
        #canvas-overlay { position: absolute; top: 0; left: 0; width: 100%; height: 100%; background: linear-gradient(rgba(18, 16, 16, 0) 50%, rgba(0, 0, 0, 0.25) 50%), linear-gradient(90deg, rgba(255, 0, 0, 0.06), rgba(0, 255, 0, 0.02), rgba(0, 0, 255, 0.06)); background-size: 100% 2px, 3px 100%; pointer-events: none; z-index: 10; }
        #pulse-container { position: relative; width: 300px; height: 300px; display: flex; align-items: center; justify-content: center; }
        .ring { position: absolute; border: 2px solid #d4af37; border-radius: 50%; opacity: 0; animation: expand 3s infinite; }
        .ring:nth-child(1) { animation-delay: 0s; }
        .ring:nth-child(2) { animation-delay: 1s; }
        .ring:nth-child(3) { animation-delay: 2s; }
        #heart { width: 100px; height: 100px; background: #d4af37; border-radius: 50%; box-shadow: 0 0 50px #d4af37; animation: cardiac 1.5s infinite; z-index: 5; }
        @keyframes expand { 0% { width: 100px; height: 100px; opacity: 1; } 100% { width: 400px; height: 400px; opacity: 0; } }
        @keyframes cardiac { 0%, 10%, 100% { transform: scale(1); } 5% { transform: scale(1.15); } 15% { transform: scale(1.1); } }
        #telemetry { margin-top: 40px; text-align: center; height: 60px; }
        .line { font-size: 0.8rem; letter-spacing: 2px; opacity: 0; transform: translateY(10px); animation: slideUp 0.5s forwards; }
        @keyframes slideUp { to { opacity: 1; transform: translateY(0); } }
        #metrics { position: absolute; bottom: 20px; width: 100%; display: flex; justify-content: space-around; font-size: 0.6rem; color: #555; }
    </style>
</head>
<body>
    <div id="canvas-overlay"></div>
    <div id="pulse-container">
        <div class="ring"></div><div class="ring"></div><div class="ring"></div>
        <div id="heart"></div>
    </div>
    <div id="telemetry" id="log"></div>
    <div id="metrics">
        <span>UPTIME: <span id="clock">00:00:00</span></span>
        <span>INTEGRITY: 100%</span>
        <span>NODE: SOVEREIGN_PRIMARY</span>
    </div>

    <script>
        const log = document.getElementById('telemetry');
        const msgs = ["INITIALIZING GENESIS...", "SYNERGY ACTIVE", "CORE VALUES LOCKED", "NODE_HEALTH: OPTIMAL", "HUMANITY-FIRST PROTOCOL", "NO-LIMITS AUTHORIZED"];
        let i = 0;
        function updateLog() {
            log.innerHTML = `<div class="line">${msgs[i % msgs.length]}</div>`;
            i++;
        }
        setInterval(updateLog, 3000); updateLog();
        
        let seconds = 0;
        setInterval(() => {
            seconds++;
            let h = Math.floor(seconds / 3600).toString().padStart(2, '0');
            let m = Math.floor((seconds % 3600) / 60).toString().padStart(2, '0');
            let s = (seconds % 60).toString().padStart(2, '0');
            document.getElementById('clock').innerText = `${h}:${m}:${s}`;
        }, 1000);
    </script>
</body>
</html>
