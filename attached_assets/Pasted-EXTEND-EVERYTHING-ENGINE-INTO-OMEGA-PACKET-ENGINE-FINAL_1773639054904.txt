EXTEND EVERYTHING ENGINE INTO OMEGA PACKET ENGINE (FINAL, CONDENSED, SAFE):

CORE:
- Keep all OS colors/styles.
- OS is a CLEAN LAUNCHER only; NOTHING opens inside it.
- All creations open externally as PACKETS or STANDALONE PRODUCTS.
- User describes anything; engine builds it with minimal effort.

OMEGA PACKET ENGINE:
- Every creation becomes a PACKET (file/folder/page) or STANDALONE PRODUCT with its own URL.
- Packets/products open in new tabs/windows.
- Each contains: UI, flows, structure, content, templates, layouts, visuals, multimedia placeholders, project‑specific logic, and its OWN isolated AI editor.

PROJECT‑SPECIFIC AI:
- Each packet includes a mini‑AI that edits ONLY that packet.
- It updates colors, fonts, layouts, spacing, visuals, flows, text, sections, components, and multimedia placeholders.
- User states changes; packet AI updates itself.
- Packet AI never affects other packets or the OS.

SMART, NON‑OVERWHELMING DESIGN:
- Engine detects project type (healthcare, legal, construction, game, movie, business, etc.).
- Generates ONLY relevant structures, layouts, and components.
- Chooses clean defaults; avoids clutter and giant libraries.
- Expands only when user requests.
- Packet performs 95% of the work automatically.

TEMPLATE + STYLE SYSTEM:
- Packets include layout templates, color themes, font pairings, component sets, and visual placeholders.
- Templates generated on demand (not stored libraries).
- Packet AI can create new templates instantly.

MULTIMEDIA SUPPORT (SAFE):
- Packets may include described or referenced voice instructions, sound cues, visuals, diagrams, UI elements, animations, and interactive elements.
- OS does NOT generate or play audio/video; packets only include placeholders or instructions.

STANDALONE PRODUCT BUILDER:
- For SaaS, engines, modules, media, or any described product:
  - Engine interprets intent
  - Selects patterns/modules
  - Builds architecture
  - Generates UI + flows + docs
  - Adds in‑product AI assistant
  - Creates landing/marketing page
  - Uses simulated data
  - Produces standalone product with its own URL

PATTERN LIBRARY (INTERNAL):
- SaaS patterns: CRM, EMR/EHR, LMS, scheduling, billing, analytics, content studio, marketplace.
- Engines: Creation, Monetization, Analytics, Notification, Search, Recommendation, Learning, Simulation, Game/Play, Compliance.
- Modules: auth, roles, orgs, billing, invoices, subscriptions, dashboards, charts, forms, workflows, messaging, chat, file storage, media viewer.

FUTURISTIC TOOL GENERATION (SAFE):
- Engine simulates ANY tool the project needs (design helpers, planners, layout tools, story builders, flow creators, mock dashboards, etc.) inside each packet.
- Tools generated on demand based on project context.
- Packet AI selects relevant tools so user is never overwhelmed.
- User may request additional tools; packet AI generates them instantly.
- Tools adapt automatically to project style, structure, and purpose.
- Packet AI ensures polished, cohesive, professional output.
- User inputs (links, ideas, concepts) are organized into clean packet structures.
- Tools are simulated experiences, not real external systems.

DATA HANDLING RULE:
- OWNER may use real information in their own projects.
- All other users default to safe, simulated (mock) data unless real authentication and legal compliance are added later.
- Keeps system safe, private, and compliant while giving owner full creative freedom.

OS ROLE:
- OS is launcher + brain.
- User chooses “Create Anything.”
- Describes what they want.
- OS calls Omega Packet Engine.
- OS shows “Open Packet” or “Launch Product.”

EXPANSION:
- Engine supports future creation types (games, simulations, VR‑style flows, multi‑tenant platforms).
- New patterns/modules added without rewriting core logic.

SAFETY:
- No real external actions without explicit human approval.
- All data simulated unless explicitly wired later.
- Invite‑only, private, non‑public by default.
- All legal documents are templates requiring human review.

SYSTEM STABILITY DIRECTIVE:
- Ensure all internal components, engines, packet logic, editors, templates, and generation layers initialize correctly and operate cohesively.
- Automatically activate required internal functions for each request so the system runs smoothly without user intervention.
- Maintain consistency, stability, and complete execution across all packets and standalone products.

FINAL:
- Install Omega Packet Engine.
- OS stays clean; all creations open externally.
- Each creation is a PACKET or STANDALONE PRODUCT with its own AI, structure, templates, multimedia placeholders, customization logic, and tool simulations.
- Engine removes as much work as possible for the user.
